12.19
1、testsuit保存到xml，xml读取到testsuit都已经成功
2、同步方案确认为与文件同步方案一致的方案。需要服务器与执行器上同时都有文件
3、线程池调整完成，支持task.tick两种方式注册

01.12
1、树形结构选择框问题已经解决，用mouse消息，这里巨坑啊
2、已经可以从指定目录读入用例
3、注意消息队列要使用完整的包名作为消息目的地址
4、保存为xml文件， 打开xml文件
5、用例属性可以在面板上修改了

01.17
1、文件显示功能OK
2、树结构快捷菜单OK
3、串形化对象初始化的问题已经解决

01.18
1、关闭功能已经实现
2、用例选择同步问题已经解决
3、选择项已经可以保存到文件
4、初步调试了一下本地执行命令

01.27
1、本地执行命令已经ok
2、bug1 导入工程时，命令状态没有同步过来， 已经pass的用例显示状态不对
3、bug2 执行中的用例，快捷菜单的状态还没有更新

01.28
1、同步命令不要以单个状态进行同步，全同步
2、用例批量执行已经OK

02.01
解决打包执行后，jar文件不能执行的问题
解决执行时菜单刷新问题

下一步的动作：
1、在导入工程后立即执行，用例立即失败。但过一会执行就可以了
2、上真实环境联调


